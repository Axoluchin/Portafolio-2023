<main class="cover">
  <img
    alt="avatar"
    src="./images/avatar.png"
    class="avatar aspect-auto w-full h-72 md:w-1/3 md:h-auto object-contain"
  />
  <div class="text">
    <p class="font-bold text-lg -rotate-6">
      Hola, soy Damián y
      <br />
      <span class="text-uva">Programar es la magia del siglo XXI</span>
    </p>
    <h1 class="font-bold text-3xl md:text-4xl my-5">
      Developer Frontend & Mobile <i class="bx bxl-react"></i><i
        class="bx bx-mobile-alt"></i>
    </h1>
    <p class="text-lg">
      Me gusta desarrollar productos y programas en el que los usuarios puedan
      interactuar, el código es una herramienta para llegar a la meta.
    </p>
  </div>
</main>

<style>
  .avatar {
    transform: translatey(0px);
    animation: float 5s ease-in-out infinite;
  }
  .cover {
    margin: 10rem 0;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 5rem;
  }

  .bxl-react {
    color: #61dafb;
  }
  .bx-mobile-alt {
    background: #2fd37d;
    background: radial-gradient(
      ellipse farthest-corner at bottom left,
      #2fd37d 0%,
      #ffffff 100%
    );
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  @keyframes float {
    0% {
      box-shadow: 0 5px 15px 0px rgba(0, 0, 0, 0.6);
      transform: translatey(0px);
    }
    50% {
      box-shadow: 0 25px 15px 0px rgba(0, 0, 0, 0.2);
      transform: translatey(-20px);
    }
    100% {
      box-shadow: 0 5px 15px 0px rgba(0, 0, 0, 0.6);
      transform: translatey(0px);
    }
  }

  @media only screen and (max-width: 800px) {
    .cover {
      display: block;
    }
    .cover {
      margin: 2rem 0 6rem;
    }
  }
</style>
